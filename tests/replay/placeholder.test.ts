import { describe, expect, it } from "vitest";

describe("v2 replay placeholder", () => {
  it("keeps the replay CI job green until v2 fusion replay lands", () => {
    expect(true).toBe(true);
  });
});
